# BKN Visitor App - Environment Configuration
# Copy this file to .env and fill in secure values

# 1. Database Configuration
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=postgresql://akuarium:password_here@10.100.11.226:30122/logbook-dev

# 2. Security
# Generate secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-secure-random-key

# 3. CORS Configuration (Frontend URLs)
# No trailing slashes. Comma separated for multiple domains.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80

# 4. Admin Setup
# Set to 'true' ONLY when creating the first admin account
ALLOW_SETUP_ADMIN=false
