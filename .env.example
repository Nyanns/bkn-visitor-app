# BKN Visitor System - Environment Configuration
# Copy this file to .env and fill in your values

# === Security Configuration ===
# CRITICAL: Change this to a long, random string in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-this-to-a-long-random-secret-key

# === Database Configuration ===
# SQLite (Development):
DATABASE_URL=sqlite:///./database.db

# PostgreSQL (Production):
# DATABASE_URL=postgresql://user:password@localhost:5432/bkn_visitor

# === Admin Setup ===
# IMPORTANT: Set to false after creating initial admin!
# Only set to true during first-time setup or when creating admin via API
ALLOW_SETUP_ADMIN=false

# === JWT Configuration (Optional) ===
# Token expiration in minutes (default: 480 = 8 hours)
# ACCESS_TOKEN_EXPIRE_MINUTES=480

# === File Upload Configuration (Optional) ===
# Maximum file size in MB (default: 10)
# MAX_FILE_SIZE_MB=10

# === CORS Configuration (Optional) ===
# Allowed origins for CORS (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174
