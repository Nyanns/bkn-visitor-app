# ========================================
# BKN Visitor Management System - Docker Environment
# Last Updated: January 14, 2026 (v2.0.0)
# ========================================
# Copy this file to .env and fill in secure values
# JANGAN commit file .env ke repository!
# ========================================

# PostgreSQL Database Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
POSTGRES_DB=bkn_visitor

# Backend Security
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-256-bit-secret-key-here-change-this-in-production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80

# Admin Setup (Enable ONLY for initial setup)
ALLOW_SETUP_ADMIN=false

# Optional: Admin credentials (for initial setup via script if needed)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=secure_admin_password

# ========================================
# Production Settings (Optional)
# ========================================

# Enable HTTPS redirect (set to true in production with SSL)
# FORCE_HTTPS=false

# Rate limiting (handled by code/slowapi defaults)
